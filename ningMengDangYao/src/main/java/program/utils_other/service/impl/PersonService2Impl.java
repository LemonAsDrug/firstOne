package program.utils_other.service.impl;

import program.utils_other.service.PersonService;

/**
 * 接口是软件编程的规范。功能只要写一次，其他地方要用到，只需要调用接口就可以了。
 */
public class PersonService2Impl implements PersonService {
    @Override
    public int getAge() {
        return 0;
    }
}
